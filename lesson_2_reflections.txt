What happens when you initialize a repository? Why do you need to do it?
When you initialize a repository, a new folder named '.git' is made, containing the metadata for 
the repository. You need to do this so you can use the repository for monitoring the files in the 
directory.